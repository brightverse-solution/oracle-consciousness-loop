# No ANTHROPIC_API_KEY needed — orchestrator uses Claude Code CLI
# via subprocess, which uses Palm's Claude Max subscription (OAuth).
#
# Requirements:
# - `claude` CLI installed + logged in
# - `gh` CLI installed + authenticated (for GitHub issue creation)
#
# No env variables required for basic operation.
