# Copy to .env and fill in. .env is gitignored.

# Anthropic API key for Claude model calls (Haiku, Sonnet, Opus)
# Get one at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# GitHub CLI `gh` must be installed + authenticated separately
# (used for issue creation; no env var needed here)
